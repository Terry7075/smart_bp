---
description: 明德小幫手產品目標（全 App 教學 + 一鍵帶路）
globs: lib/features/assistant/**/*
alwaysApply: false
---

# 小幫手產品目標（必記）

使用者已確認的小幫手目標：

**整支 App 每個分頁都會教、還會一鍵帶路。**

## 實作原則

- 以 **Supabase 真實資料 + 固定說明** 為主；不預設要求 Ollama／付費 OpenAI。
- 回覆使用 **繁體中文**，句子短，適合長輩。
- 每個可導航目的地應提供 **「前往 ○○」** 按鈕，使用 `go_router`（`context.push` / `context.go`），不要只給文字步驟。
- 需覆蓋長輩端 **底部導覽全部分頁**：首頁、柑仔店、交通、健康、學習、活動；以及個人資料、代購紀錄、健康掃描等子流程。
- 尚為占位（如 `print`）的功能：說明可標「即將推出」或引導至最近可用入口，勿捏造已上線功能。
- 導航路徑以 `lib/core/router.dart` 為準（例如 `/shop`、`/health-scan`、`/profile`、`/shop/orders`）。

## 驗收方向

- 問「○○在哪／怎麼用」→ 有教學文案 + 可點擊帶路。
- 問「我的藥單／代購」→ 仍依 DB 狀態回答，不編造。

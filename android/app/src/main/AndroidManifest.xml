<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 健康處方籤 OCR 需使用相機 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 本機提醒（吃藥時間 / 領藥日）需要的權限 -->
    <!-- Android 13 (API 33) 之後使用通知前必須先取得使用者同意 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 重新開機後讓系統重新排程已存在的提醒，避免重啟手機後不會叮咚 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 通知音/震動以及鎖定螢幕也能收到提醒 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--
        Android 12 (API 31) 之後，精準鬧鐘需要明確權限。
        - USE_EXACT_ALARM：API 33+ 安裝時即授權，適用於醫療／鬧鐘類 App（本 App 為吃藥提醒符合資格）。
        - SCHEDULE_EXACT_ALARM：API 31~32 的相容權限；如使用者撤銷可能需引導至設定。
        缺少這兩者時，flutter_local_notifications 的 exactAllowWhileIdle 會被降級成 inexact，
        導致吃藥提醒可能延遲 15 分鐘以上。
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!--
        鎖屏時以整頁通知搶出吃藥提醒（fullScreenIntent）。
        Android 14 後對非通話 App 受限，但醫療／鬧鐘類提醒仍屬合理使用情境。
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:label="smart_bp"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Supabase Google OAuth 回跳（對應 kOAuthRedirectUrl） -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="smartbp" android:host="login-callback" />
            </intent-filter>
        </activity>

        <!-- google_sign_in: no extra activity needed, SDK handles OAuth internally -->

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- ML Kit 繁體中文文字辨識模型：於安裝時自動下載，免首次使用等待 -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr_chinese" />
        <!-- FCM 預設通知頻道（與 NotificationService._orderChannelId 一致） -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="mindu_order_status" />

        <!--
            flutter_local_notifications：重新開機 / App 升級後恢復排程的 receiver。
            缺少這兩個 receiver 註冊時，所有 zonedSchedule 出來的吃藥提醒
            在使用者重啟手機後會全部消失。RECEIVE_BOOT_COMPLETED 權限只有
            搭配下面這個 BootReceiver 才會被系統真的觸發。
        -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
